import React from 'react';

function article(){
    const articleTitle = 'single Article';
    return (
        <div>
            <h1>{articleTitle}</h1>
        </div>
    );
}

export default article;